# ── 离线载荷：各类运行库安装包、DirectX 运行库与 DLL、第三方工具包 ──
# 体积大（约 520 MB），且属于微软/第三方的可再分发组件，不随源码分发。
# 需要时请跑 tools\Fetch-OfflinePack.ps1 抓取 —— 该脚本【已收录进本仓库】：
# 仓库里只放"下载这些库的脚本"，不放库本体。
offline/

# ── 本机运行产物：与具体机器绑定，没有分发价值 ──
backup/
报告/
修复报告.txt
修复报告.html
*.log

# ── 打包输出 ──
dist/

# ── 打包产物：由 tools\Build-Package.ps1 生成在发布树里 ──
打包说明.txt

# ── 记住的扫描目录（与具体机器绑定，本机使用即可）──
scan-roots.json
