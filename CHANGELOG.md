# 变更日志

本文件记录每个版本的功能与修复。版本号见 `VERSION.txt`。

---

## 1.0.1 — 2026-09-17

### 新增：询问要搜索哪几个目录

**问题**：搜索游戏时会把每个固定磁盘整盘按深度遍历一遍，在典型机器上要访问
十几万个目录、耗时以分钟计，而真正放游戏的往往只有一两个目录 —— 大部分时间
花在了 `Windows`、`Program Files`、`AppData` 这些不可能有游戏的树上。

**做法**：

- 首次运行会先列出候选目录（上次选择 / 常见位置 / 各固定盘）让用户挑选，
  支持多选、自定义路径、跳过搜索、或直接回车只扫常见位置。
- 选择记入 `scan-roots.json`（与机器绑定，已加入 `.gitignore`），下次直接复用。
- 只扫常见位置且没找到游戏时，会再问一句要不要整盘搜一次，不必自己重跑。
- 非交互环境（输入被重定向、计划任务、`-NoScanPrompt`）自动跳过提问，
  走便宜的那条路，不会卡住。
- 每次搜索结束后打印**访问目录数 / 剪枝跳过数 / 耗时**，让代价可见。

**同时做的两项性能优化**：

1. **每个目录只列举一次**。旧实现先枚举所有子目录、再对每个目录单独列一次
   `th*.exe`，等于每个目录两次 I/O；现在把两件事合并成同一次列举。
2. **按目录名剪枝**。实测同一条 `C:\` 搜索：不剪枝访问 30,000 个目录、
   46.5 秒仍未跑完（撞上限中断）；剪枝后只访问 316 个目录、0.4 秒完成。

**新增参数**：

| 参数 | 作用 |
| --- | --- |
| `-ScanRoot` | 只在这些目录里搜索，不提问。支持数组、分号分隔、空格分隔三种写法 |
| `-NoScanPrompt` | 不提问，直接按默认范围（常见位置）搜索 |
| `-ScanDepth` | 搜索深度，默认 3 |

**修掉的一个参数传递缺陷**：PowerShell 把数组传给原生进程时会拼成一个以空格
分隔的字符串，所以经 `Run.ps1` 传 `-ScanRoot "D:\game","E:\other"` 到达引擎时
是**一个**不存在的路径，结果是"访问 0 个目录、什么都没找到"且不报错。
`ConvertTo-TrdScanRootList` 现在按「整体 → 分号/逗号 → 空白」逐级尝试并只接受
真实存在的路径，三种写法都能正确工作；一个都解析不出来时会显式告警并退回默认范围。

---

## 1.0.0 — 2026-09-16

首个发布版本。以下按主题列出，末尾附提交对照，便于回溯到具体改动。

### 体检与诊断

- **依赖完整性**：直接解析游戏 exe 的 PE 导入表，逐个确认依赖 DLL 能否找到。
  含 ApiSet（`api-ms-win-*` 这类磁盘上不存在真实文件的虚拟 DLL）识别，
  以及调试变体名（如 `d3d8d.dll`）过滤，避免据此误报缺失。
- **DirectX**：111 项组件清单，逐项核对 32 位与 64 位目录；
  另有 Direct3D / DirectDraw 硬件加速开关检测（被误设为软件渲染时，
  依赖检查会全部通过，但游戏极慢或直接起不来）。
- **VC 运行库**：2005 / 2008 / 2010 / 2012 / 2013 / 2015-2022，
  x86 与 x64 **分别**核实，同时取注册表、系统目录、WinSxS 三处证据。
- **键盘暴走专项**：13 类检查（输入法/键盘布局、辅助功能四件套、
  内核级 Scancode Map、键盘鼠标过滤驱动、虚拟/转换键盘设备、多键盘、
  手柄摇杆漂移、蓝牙 HID、设备错误码、按键重复率、DirectInput 按程序映射、
  输入注入类进程、显示时序）。
- **整机信息**：系统/CPU/内存/显示器/存储/网络/输入设备/PnP 快照/
  问题设备/电源/热区/补丁/启动项/关键服务/运行库，输出文本、HTML、JSON 三种格式。
- **输入诊断快照**：导出 JSON 原始数据（不只是结论），便于后续分析或接入插件。

### 修复

- 所有修复遵循**先备份、可回滚**：文件类改动整文件备份；
  注册表改动除整分支导出外，还逐值记录——只做整分支导出是不完备的，
  `reg import` 只能合并/覆盖，无法删除修复过程中新增的值，
  会导致回滚后新增项残留、用户以为已还原其实没有。
- 可自动修复：部署 DirectX 组件、安装 VC 运行库、兼容性标志、
  重置 DirectInput、禁用辅助功能快捷键、添加英文键盘布局、
  移除 Scancode Map、硬件加速开关、解锁文件安全标记、目录权限、
  迁移到纯 ASCII 路径、字体安装、音频服务、MSI 服务、生成启动器、
  部署 dgVoodoo2 / Locale Emulator。
- **启动验证**：跟踪 vpatch 启动的子进程与窗口句柄，
  并把 NTSTATUS 退出码翻译成人话
  （`0xC0000135` 缺 DLL、`0xC000007B` 位数不符、`0xC0000142` DLL 初始化失败等）。

### 健壮性（这些不是理论问题，都是实际踩到并修掉的）

- **`.ps1` 强制 UTF-8 带 BOM**。中文 Windows 上的 PowerShell 5.1 会把无 BOM 的
  UTF-8 当 GBK 解码，中文变乱码并直接破坏语法——脚本在工具自己的错误处理
  跑起来之前就已经解析失败了。`Run.ps1` 因此写成纯 ASCII，并在启动时自愈 BOM。
- **WOW64 重定向规避**。32 位进程在 64 位系统上访问 `C:\Windows\System32`
  会被静默改写成 `SysWOW64`，读 `HKLM\SOFTWARE\...` 会被改写到
  `SOFTWARE\WOW6432Node`，两者都不报错。实测同一路径
  `System32\d3dx9_43.dll` 在 64 位进程下是 2,401,112 字节（x64 真身），
  在 32 位进程下变成 1,998,168 字节（x86）。后果是"以为在查 64 位组件，
  实际查的是 32 位文件"，最后输出一份看着正常、结论全错的报告。
  处理方式：入口优先用 `Sysnative` 拉起**原生位数**的 PowerShell；
  系统目录统一走 `Get-TrdSystemDir`；注册表统一用 `OpenBaseKey` 显式指定视图。
- **不硬编码 `SysWOW64`**。32 位 Windows 上根本没有 WOW64 层，
  32 位 DLL 就在 `System32` 里。硬编码会让工具把所有组件报成缺失，
  把一台健康的 32 位机器判成需要大修。
- **注册表不依赖进程默认视图**。VC++ 运行库分位数注册，
  依赖默认视图会漏掉一半证据，进而误报"VC 运行库缺失"并触发毫无必要的重装。
- **`Start-Process -PassThru` 的 `ExitCode` 不可靠**（成功也可能返回 -1），
  需要真实退出码时改走 `System.Diagnostics.Process`。
- **兼容 PowerShell 3.0 / Windows 7**：PS5 专有 cmdlet
  （`Get-FileHash`、`Expand-Archive`、`Get-CimInstance`、`Get-ChildItem -Depth`）
  全部具备降级实现；不含 PS7 专有语法、PS4+ 的 `.Where{}` 方法语法
  或 .NET Core 专有 API；抓取脚本显式启用 TLS 1.2
  （Win7 默认可能只有 TLS 1.0，直连 HTTPS 会失败）。

### 修复的具体缺陷

- **输入诊断静默失败造成假阴性**。`Get-TrdInputDiagnostics` 的调用原本是
  `catch { }`，一旦抛异常，整个「输入子系统」分类会从报告里消失，
  用户会读成"没有输入问题"——而实际上这项检查根本没跑成。
  对暴走这种重点专项来说，静默跳过比报错危险得多。
  现在会输出 `INPUT_DIAG_FAILED`，写明"这不代表没有问题，只代表这次没查成"
  并附错误信息。
- **DirectX 官方安装路径被自身校验挡死**。`DX9_REDIST_JUN2010` 的 `Files`
  里同时列了两个候选（已解包的小安装器 + 下载的归档），
  但一个包只有一个 `Sha256` 字段，它描述的是归档。跑过解包之后，
  查找先命中约 506 KB 的小文件，再拿它跟 95 MB 归档的哈希比对，
  必然报"哈希不符"，而安装流程遇到哈希不符会按设计中止。
  修法是把"下载物"和"解包产物"分开：清单只保留下载物；
  解包产物由代码按约定位置 `offline\DirectX\dxsetup\` 探测。
- **打包载荷排除清单不完整**。`Build-Package.ps1` 的清单写的是
  `offline\DirectX`，而代码实际读取 `offline\DX_DLL`，
  两者不一致会让 lite 包把近 190 MB 的 DirectX DLL 一起打进去，
  彻底违背"lite = 仅工具本体"的约定，而体积异常又很容易被忽略。
- **键盘/鼠标过滤驱动误报**。检测硬编码 `System32\drivers`，
  32 位进程下被重定向到 `SysWOW64\drivers`，找不到 `kbdclass.sys` 等，
  于是在一台干净机器上报出「高危：挂了第三方过滤驱动」。
  顺带修掉该处变量遮蔽（`foreach ($base ...)` 覆盖了注册表类键）。
- **报告泄漏内部别名路径**。为绕开重定向而使用的 `Sysnative` 会出现在报告里，
  但用户在资源管理器里看不到这个目录。已在显示层统一还原为 `System32`；
  早期正则要求尾随反斜杠，漏掉了行尾与中文括号前的情况。
- **命令参数与解析的几个坑**：`Start-Process -ArgumentList`（数组形式）
  不会为含空格的元素加引号；`@(Get-Content x -Raw | ConvertFrom-Json)`
  会得到一个装着真数组的单元素数组；`Test-Path`/`Remove-Item` 对含 `[ ]`
  的路径会把方括号当通配符，必须用 `-LiteralPath`。

### 打包与分发

- `tools/Build-Package.ps1` 生成 lite / full 两种包，并产出 `SHA256SUMS.txt`。
- `tools/Check-Scripts.ps1` 静态检查：BOM、引号配对（识别注释与 here-string）、
  语法解析。
- `tools/Fix-Bom.ps1` 纯 ASCII 的 BOM 规范化工具。
- `tools/Complete-DirectXRepair.ps1` 补全你本机已安装的 DirectX Repair 的
  `Data` 目录（只读写本地文件，不联网）。
- `tools/Fetch-OfflinePack.ps1` 在**有网络**的机器上一键抓齐载荷。
  清单驱动；先下 `.part` 再校验改名（断网不留半截文件）；
  SHA256 **自封**（清单没写哈希的下载后算出并回写，之后每次都校验）；
  微软/第三方二进制额外做 Authenticode 校验；
  GitHub 类来源运行时查最新发布，不把版本号写死在 URL 里。

### 验证状态（如实说明）

- **已实测**：Windows 11 家庭中文版（Build 26200）、64 位、PowerShell 5.1。
  自检 13/13；完整体检；一键修复；**东方永夜抄启动验证**
  （成功创建游戏窗口 `东方永夜抄 ～ Imperishable Night. ver 1.00d`）。
  32 位与 64 位 PowerShell 下结论**逐条完全一致**（比较过输出，不是推断）。
- **已做静态审计但未在真机执行**：Windows 7 / 8 / 8.1 与 32 位 Windows；
  PowerShell 3.0 / 4.0。相关降级实现与代码审计已完成，但没有可用的
  真机环境实跑。

### 提交对照

| 提交 | 内容 |
| --- | --- |
| `1cb8d2f` | 初始提交：v1.0.0 公开源码版 |
| `4a3d145` | 打包脚本：区分本地压缩包与 GitHub 公开版 |
| `0fbe48b` | 打包脚本：修正载荷目录清单与打包说明 |
| `04d93eb` | 修复输入诊断静默失败造成的假阴性 |
| `e7f9ac4` | 修复 DirectX 官方安装路径被自身校验挡死的问题 |
| （本次） | 收录 `tools/Fetch-OfflinePack.ps1`，同步文档与变更日志 |
