# ── 离线载荷：各类运行库安装包与 DirectX DLL ──
# 体积大（约 200 MB），且属于微软/第三方的可再分发组件，
# 不随源码分发。需要时请自行获取并放进 offline\ 目录。
offline/

# ── 本发行版刻意不含"可自动联网下载并自动补全"的脚本 ──
# 该脚本会在有网络的机器上抓取各类运行库。为了满足
# "不含可自动联网下载自动补全的文件（如各类库）"的要求，
# 公开版不收录它，请手工获取安装包。
tools/Fetch-OfflinePack.ps1

# ── 本机运行产物：与具体机器绑定，没有分发价值 ──
backup/
报告/
修复报告.txt
修复报告.html
*.log

# ── 打包输出 ──
dist/

# ── 打包产物：由 tools\Build-Package.ps1 生成在发布树里 ──
打包说明.txt
